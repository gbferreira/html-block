export {
  HtmlBlockBoard,
  type HtmlBlockBoardHandle,
  type HtmlBlockBoardProps,
} from "./Board.js";
/** Same component; preferred import when you want `<Board />` in JSX without clashing with the `Board` class from the root entry. */
export { HtmlBlockBoard as Board } from "./Board.js";
