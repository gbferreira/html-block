/** Default swatches for rectangle fills. */
export const DEFAULT_COLORS = [
  "#ffffff",
  "#fde68a",
  "#fecaca",
  "#bfdbfe",
  "#bbf7d0",
  "#e9d5ff",
  "#fed7aa",
  "#cbd5e1",
];

/** Default swatches for canvas background picker. */
export const DEFAULT_BACKGROUND_COLORS = [
  "#ffffff",
  "#f8fafc",
  "#fef9c3",
  "#dbeafe",
  "#dcfce7",
  "#fae8ff",
  "#fee2e2",
  "#0f172a",
];

export const DEFAULT_FONT_SIZES = [12, 14, 16, 18, 24, 32, 48];

/** Fallback rect fill when palettes are empty or invalid. */
export const DEFAULT_RECT_FILL_FALLBACK = "#ffffff";

/** Default selected font size (px) before user override. */
export const DEFAULT_RECT_FONT_SIZE = 14;

/** Default SVG rect border appearance. */
export const DEFAULT_BORDER_COLOR = "#000000";
export const DEFAULT_BORDER_WIDTH = 1;

export const DEFAULT_FONT_FAMILY =
  "ui-sans-serif, system-ui, -apple-system, Segoe UI, sans-serif";

export const DEFAULT_INITIAL_BOARD_WIDTH = 1200;
export const DEFAULT_INITIAL_BOARD_HEIGHT = 800;

export const DEFAULT_GROW_STEP = 200;

export const DEFAULT_MIN_BLOCK_WIDTH = 40;
export const DEFAULT_MIN_BLOCK_HEIGHT = 28;

export const DEFAULT_STORAGE_KEY = "html-block:default";

export const DEFAULT_LINE_COLOR = "#0f172a";
export const DEFAULT_LINE_WIDTH = 2;
